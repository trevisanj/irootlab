%> Returns optimal number of features to use spline coefficients representation as "pre-processing"
%> @todo Check after I run the average ClArchSel's if this is really the number
function a = get_spline_optimalnf()
a = 40;
