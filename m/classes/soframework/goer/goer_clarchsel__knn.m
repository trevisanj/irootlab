%> architecture optimization for the knn classifier
classdef goer_clarchsel__knn < goer_1i
    methods
        function o = setup(o)
            o.classname = 'clarchsel_knn';
        end;

        function d = customize_session(o, d)
            % Make your experiments here
            % It is possible to do experiments here
        end;
    end;
end
