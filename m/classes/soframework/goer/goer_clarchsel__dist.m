%> architecture optimization for the dist classifier
classdef goer_clarchsel__dist < goer_1i
    methods
        function o = setup(o)
            o.classname = 'clarchsel_dist';
        end;

        function d = customize_session(o, d)
        end;
    end;
end
