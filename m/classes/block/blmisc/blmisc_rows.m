%> @brief Base class for blocks that change a dataset row order.
classdef blmisc_rows < blmisc
    methods
        function o = blmisc_rows(o)
            o.classtitle = 'Row';
        end;
    end;
end

