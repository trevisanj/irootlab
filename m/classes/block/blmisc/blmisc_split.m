%> @brief Base class for blocks that split one dataset into many
classdef blmisc_split < blmisc_rows
    methods
        function o = blmisc_split(o)
            o.classtitle = 'Split';
        end;
    end;
end

